let config = {
    server: 'localhost',
    authentication: {
        type: 'default',
        options: {
            userName: 'Anton', 
            password: 'ewqqwe' 
        }
    },
    options: {
        database: 'node_lab14',
        encrypt: false,
    }
}

module.exports = config